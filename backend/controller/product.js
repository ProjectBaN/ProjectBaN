require("dotenv").config();

const writeProduct = async (req, res, next) => {
  // 카테고리
  // 상품이름
  // 상품설명
  // 상품 이미지
  // 옵션: 이름, 가격,재고,할인율
  //[ 옵션1,옵션2,옵션3...]
};

module.exports = { writeProduct };
